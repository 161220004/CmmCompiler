#include "Assembler.h"

/* 变量+临时变量内存位置表（有序数组） */
MemElem* memList = NULL;

/* 生成MIPS32目标代码 */
void generateMIPS(char* fileName) {
  if (!isError() && isLab(4)) {
    if (IRList != NULL) {
    }
  }
}
